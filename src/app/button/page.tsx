"use client";

import { ButtonContent } from "@/components/v1/winv";

export default function ButtonPage() {
  return (
    <main className="relative bg-stone-50 dark:bg-background">
      <ButtonContent setProgress={() => {}} />
    </main>
  );
}
